import React from "react";
import { Title } from "..";
import Layout from "../../layout";

const Loading = () => (
  <Layout>
    <Title name="HOME" />
    <p>Loading...</p>
  </Layout>
);

export default Loading;
